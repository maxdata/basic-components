{# def
    id: str = "",
    method: str = "POST",
    action: str = "",
    className: str = ""
#}

<form id="{{ id }}" method="{{ method }}" action="{{ action }}" class="{{ className }}" {{ attrs.render() }}>
  {{ content }}
</form>
