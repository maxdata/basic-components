{# def
    id: str = "",
    className: str = "",
#}
<!-- DialogOverlay -->
<div id="{{ id }}" x-show="open"
    x-transition:enter="transition ease-out duration-300"
    x-transition:leave="transition ease-in duration-300"
    class="fixed inset-0 z-50 flex items-center justify-center bg-black opacity-70"
{#    class="fixed inset-0 bg-black opacity-70 z-400"#}
></div>
